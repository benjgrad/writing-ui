#!/bin/sh

echo "Running Next.js production build (includes type check)..."
npm run build

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Build failed. Please fix the errors before committing."
  exit 1
fi

echo "✅ Build passed"
